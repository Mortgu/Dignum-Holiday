export default function LoginLayout({children}) {
    return (
        <html>
            <head></head>
            <body>
                <div>{children}</div>
            </body>
        </html>
    );
}
